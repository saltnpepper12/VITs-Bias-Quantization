# Core deep-learning stack
torch>=2.0
torchvision>=0.15
transformers>=4.35
accelerate>=0.24

# ONNX export, runtime & quantization
onnx>=1.14
onnxruntime>=1.16          # use onnxruntime-gpu on CUDA machines

# Data & dataset
datasets>=2.14
pandas>=2.0
pyarrow>=12.0
pillow>=9.5
numpy>=1.24

# Cloud training/inference (FairFace runs were executed on Modal A100s)
modal>=0.55

# Analysis & figures (used to produce the charts in /assets)
matplotlib>=3.7
tqdm>=4.65
